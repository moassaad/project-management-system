spring:
  application:
    name: backend-spring-boot
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/pms}
    username: ${SPRING_DATASOURCE_USERNAME:pms}
    password: ${SPRING_DATASOURCE_PASSWORD:pms}

server:
  port: 8080

logging:
  level:
    com.projectmanagementsystem: INFO
    org.springframework: INFO

# Test profile keeps placeholder for Testcontainers override (BE-S002-08)
# No JPA/Flyway yet — introduced in BE-S002-03
