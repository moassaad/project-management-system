# Public frontend configuration.
# Vite exposes VITE_* variables to the browser bundle, so never put secrets here.
# See docs/architecture/system-architecture.md (Frontend Environment Strategy).
VITE_API_URL=http://localhost:8080/api/v1
